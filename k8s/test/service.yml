apiVersion: v1
kind: Service
metadata:
  name: spring-boot
  labels:
    app: spring-boot
spec:
  clusterIP: 10.23.246.164
  selector:
    app: spring-boot
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer