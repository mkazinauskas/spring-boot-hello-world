apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring-boot
  name: spring-boot
  namespace: default
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: spring-boot